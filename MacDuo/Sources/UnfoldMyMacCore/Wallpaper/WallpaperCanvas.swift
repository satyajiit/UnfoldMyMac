import Foundation

/// Template layout coordinates are fractions of this design canvas; every display fits it letterboxed.
public enum WallpaperCanvas {
    public static let width = 1600.0
    public static let height = 1000.0
}
