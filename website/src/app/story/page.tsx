import Image from "next/image";
import { PageIntro, DownloadCTA, Sources, Breadcrumbs } from "@/components/shared";
import { pageMetadata } from "@/lib/metadata";
import { site, sources } from "@/lib/site";
export const metadata = pageMetadata("/story/");
export default function StoryPage() { return <div className="container"><PageIntro label="The story" title="Apparently, closing a laptop needed art direction." description="How one lid effect grew into a desktop full of little worlds." />
  <article className="prose story-prose"><p>I started by trying to bring the iPhone Duo effects to the Mac. I wanted that same sense of movement, but tied to something you could physically do with the laptop. Playing with the MacBook’s sensors led to using the lid angle to drive the transition.</p><h2>Then I added more effects.</h2><p>A blur became curtains, image reveals, flowing ribbons, and little characters peeking at the desktop. Apparently, closing a laptop needed art direction.</p><figure className="wide-figure"><Image src="/media/origin-comic.webp" alt="Three comic panels: one lid effect becomes a collection, then a desktop full of live-data scenes" width={1400} height={467} sizes="(max-width: 800px) 92vw, 900px" /><figcaption>The short version of the scope creep.</figcaption></figure><h2>The lid was only the start.</h2><p>Once the rendering was there, I wanted to build a live wallpaper engine with real data behind it. CPU load could move a sculpture. Local coding activity could animate a robot. A public GitHub profile could become a city.</p><p>That grew into the wallpaper collection, with separate templates and data sources so the next idea wouldn’t require rebuilding everything.</p><h2>There’s room for your idea, too.</h2><p>UnfoldMyMac is open source under Apache 2.0. Bug reports, hardware compatibility reports, artwork, new effects, and wallpapers are welcome. If you build something, include a recording or a few images so other people can see it.</p><p>Start with the <a href={sources.developing}>extension guide</a> or <a href={`${site.repo}/issues/new/choose`}>open an issue on GitHub</a>.</p></article><Sources><p>Adapted from the <a href={`${sources.overview}#how-it-started`}>origin story in the project README</a>. Artwork and credits are documented in the <a href={sources.notices}>project notices</a>.</p></Sources><DownloadCTA /><Breadcrumbs path="/story/" title="The story" /></div>; }
